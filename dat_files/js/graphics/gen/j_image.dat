
fields {
  IPoint size;  // dimensions
  int depth;    // number of components per pixel
  ?*byte b_pixels;  // pixel data, if 8-bit components
  ?*short w_pixels;  // pixel data, if 16-bit components
}
